#include<iostream>
using namespace std;

/*thuat toan quay lui dung de giai bai toan liet ke cau hinh.
 mỗi cấu hình được xây dựng bằng cách xây dựng từng phần tử,
 mỗi phần tử được chọn bằng cách thử tất cả các khả năng.

 mã giả : 
 try (int i) {
      for( j = <kha nang 1> ; j <= <kha nang M> ; j++) { 
      if(<chap nhan kha nang j>) { 
             x[i] = <kha nang j>;
             if(i ==n) 
               <thong bao cau hinh tim duoc>
               else {
               try(i+1);*/
